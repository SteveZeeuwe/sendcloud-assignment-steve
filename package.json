{
  "name": "sendcloud-assignment-steve",
  "version": "1.0.0",
  "description": "This repository contains my (Steve Zeeuwe) process and solution of an assignment sent by Sendcloud for the role of (senior) Front-End Developer. The goal of this assignment is to provide insight into my way of working and technical skill level.",
  "main": "range-calculator.js",
  "scripts": {
    "dev": "sass --watch --update --style=expanded assets/scss:assets/css && postcss --use autoprefixer -r assets/css/*.css",
    "build": "sass --no-source-map --style=compressed assets/scss:assets/css && postcss --use autoprefixer -r assets/css/*.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "postcss": "^8.4.35",
    "postcss-cli": "^11.0.0", 
    "sass": "^1.70.0",
    "autoprefixer": "^10.4.17"
  },
  "browserslist": [
    "last 1 version",
    "> 1%",
    "ie 11"
  ]
}
